import { AppShell } from "@/src/components/shell/AppShell";
import { CommandPanel } from "@/src/components/ui/CommandPanel";
import { WarningStrip } from "@/src/components/ui/WarningStrip";
import { getStationSample } from "@/src/lib/generatedElectionData";
import { candidates } from "@/src/data/demoElectionData";

export default async function Station({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getStationSample(id);return <AppShell><div className="space-y-4"><h1 className="text-display">{s.stationName}</h1><p className="text-body text-[#bac9cc]">{s.ward}, {s.constituency} · {s.county}</p><p className="text-xs text-[#bac9cc]">Synthetic demo data. Not official election results.</p><div className="grid gap-4 xl:grid-cols-2"><CommandPanel title="Result form evidence" active><div className="h-80 rounded-lg border border-cyan-300/30 bg-[linear-gradient(180deg,rgba(0,229,255,.08),transparent),#080f11] p-3"><div className="h-full rounded border border-dashed border-white/20"/></div></CommandPanel><div className="space-y-4"><CommandPanel title="Confidence meter"><p className="text-data-lg text-cyan-200">{100-(s.anomalyFlags[0]?.severity??10)}%</p><div className="mt-3 h-2 rounded bg-white/10"><div className="h-2 rounded bg-cyan-300" style={{width:`${100-(s.anomalyFlags[0]?.severity??10)}%`}}/></div><p className="mt-3 text-xs text-[#bac9cc]">Source consensus: {s.notes[0] ?? "Validated"}</p><p className="text-xs text-[#bac9cc]">Uploaded: {new Date(s.reportedAt).toLocaleString()}</p></CommandPanel><CommandPanel title="Critical anomalies">{s.anomalyFlags.length? s.anomalyFlags.map((f)=> <WarningStrip key={f.type} text={f.note}/>) : <WarningStrip text="No critical anomalies on sample"/>}<div className="mt-2"><WarningStrip text="Human review recommended"/></div></CommandPanel></div></div><CommandPanel title="Tally breakdown"><table className="w-full text-sm"><tbody>{s.results.map((r)=><tr key={r.candidateId} className="border-b border-white/10"><td className="py-2">{candidates.find((c)=>c.id===r.candidateId)?.name ?? r.candidateId}</td><td className="py-2 text-right text-data-md">{r.votes.toLocaleString()} ({r.percent}%)</td></tr>)}</tbody></table><div className="mt-4 grid grid-cols-3 gap-3 text-xs text-[#bac9cc]"><p>Registered: {s.registeredVoters.toLocaleString()}</p><p>Votes cast: {s.ballotsCast.toLocaleString()}</p><p>Spoilt: {s.rejectedBallots.toLocaleString()}</p></div></CommandPanel><div className="flex gap-3"><button className="rounded-lg border border-cyan-300/50 bg-cyan-400/10 px-4 py-2 text-cyan-200">Approve result form</button><button className="rounded-lg border border-[#ffb4ab]/40 bg-[#93000a]/25 px-4 py-2 text-[#ffb4ab]">Requires review</button></div></div></AppShell>}
