"use client";import { AppShell } from "@/src/components/shell/AppShell";import { useState } from "react";import { mapRegions, parties } from "@/src/data/demoElectionData";import { CommandPanel } from "@/src/components/ui/CommandPanel";
export default function Maps(){const [m,setM]=useState('Results Map');const r=mapRegions[0];return <AppShell><div className="space-y-4"><div className="flex gap-2">{['Results Map','Turnout Map','Anomaly Map'].map(v=><button key={v} onClick={()=>setM(v)} className={`rounded px-3 py-2 ${m===v?'bg-cyan-500/20 text-cyan-300':'bg-[#151d1e]'}`}>{v}</button>)}</div><div className="grid gap-4 lg:grid-cols-[1fr_360px]"><div className="h-[420px] rounded-xl border border-white/10 bg-[radial-gradient(circle_at_45%_45%,rgba(82,3,213,.3),transparent_40%),#080f11]"/><CommandPanel title="Ward focus"><p>{r.ward} / {r.constituency}</p><p className="mt-2">Turnout: {r.turnout}%</p><div className="mt-3">{parties.map(p=><div key={p.id} className="flex items-center gap-2"><span className="size-2 rounded-full" style={{background:p.color}}/> {p.name}</div>)}</div></CommandPanel></div></div></AppShell>}
