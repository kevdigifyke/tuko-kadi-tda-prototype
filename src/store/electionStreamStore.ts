import { create } from "zustand";
export type RiskColor = "green" | "yellow" | "orange" | "red";
export interface LiveStation { id:string; name:string; county:string; constituency:string; ward:string; lat:number; lng:number; voters:number; turnout:number; anomalyScore:number; influenceScore:number; sentimentScore:number; queuePressure:number; violenceRisk:number; ballotVelocity:number; riskColor:RiskColor; lastUpdated:string; }
export interface StreamAlert { id:string; timestamp:string; county:string; station:string; anomalyType:string; severity:number; message:string; }
interface ElectionStreamState { liveStations:Record<string,LiveStation>; alerts:StreamAlert[]; activeAnomalies:number; lastUpdateTimestamp:string|null; updateStation:(station:LiveStation)=>void; pushAlert:(alert:StreamAlert)=>void; clearAlerts:()=>void; }
export const useElectionStreamStore = create<ElectionStreamState>((set) => ({ liveStations:{}, alerts:[], activeAnomalies:0, lastUpdateTimestamp:null, updateStation:(station)=>set((state)=>{ const nextStations={...state.liveStations,[station.id]:station}; const activeAnomalies=Object.values(nextStations).filter((s)=>s.anomalyScore>=70||s.violenceRisk>=60||s.queuePressure>=80).length; return { liveStations:nextStations, activeAnomalies, lastUpdateTimestamp:station.lastUpdated }; }), pushAlert:(alert)=>set((state)=>({ alerts:[alert,...state.alerts].slice(0,200) })), clearAlerts:()=>set({alerts:[]}) }));
